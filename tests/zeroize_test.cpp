#include "zeroize_test.h"
#include <gtest/gtest.h>

// Example test case for zeroize functionality
TEST_F(zeroize_test, BasicTest) {
    // This is a placeholder test that checks if 1 equals 1
    EXPECT_EQ(1, 1);
}